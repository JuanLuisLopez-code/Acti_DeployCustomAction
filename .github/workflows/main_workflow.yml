name: main_workflow
on:
  push:
    branches:
      - nodejs_jest_job
jobs:
  test-execution:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install -D
      - run: npm test
